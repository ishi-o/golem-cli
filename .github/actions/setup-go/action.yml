name: setup-go
description: Set up Go from the module's go.mod directive, and cache module downloads and the build cache
runs:
  using: composite
  steps:
    - uses: actions/setup-go@v5
      with:
        go-version: '1.24'
        cache: true
        cache-dependency-path: '**/go.sum'
